<?php

/**
 * Public portal routes (home, events listing, event detail).
 */
require 'portal-routes.php';

/**
 * Student-specific routes (placeholder; extend when student auth exists).
 */
require 'student-routes.php';

/**
 * Reserved for payment or booking flows if needed later (not an e-commerce checkout).
 */
require 'checkout-routes.php';
